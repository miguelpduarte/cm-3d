export const draw = (sk, props = {}) => {
    sk.background(200);

    sk.fill(255, 255, 255);
    sk.textSize(50);
    sk.textAlign(sk.CENTER);
    sk.text('Work in Progress', 0, 0);

    // sk.orbitControl();
};
